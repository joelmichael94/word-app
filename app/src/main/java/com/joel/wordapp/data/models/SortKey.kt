package com.joel.wordapp.data.models

// An Enum class that stores the SORT_BY and SORT_ORDER strings to be called in other files for consistency purposes
enum class SortKey {
    SORT_BY, SORT_ORDER
}