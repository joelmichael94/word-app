package com.joel.wordapp.data.models

// An Enum class that stores the ASCENDING and DESCENDING strings to be called in other files for consistency purposes
enum class SortOrder {
    ASCENDING, DESCENDING
}